# Required for live API exercises.
OPENAI_API_KEY=

# Verified class default model for the current practice package.
# If another model slug is announced during class, replace this value.
OPENAI_MODEL=gpt-5.4-mini
